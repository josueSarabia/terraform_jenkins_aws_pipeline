version: '3'

services:
  backend:
    image: REPO_URL_BACKEND:COMMIT_BACKEND
    container_name: backend
    ports:
      - "8081:8081"
  frontend:
    image: REPO_URL_FRONTEND:COMMIT_FRONTEND
    container_name: frontend
    ports:
      - "80:80"